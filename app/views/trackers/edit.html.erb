<h1>Editing tracker</h1>

<%= error_messages_for :tracker %>

<% form_for(:tracker, :url => tracker_path(@tracker), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', tracker_path(@tracker) %> |
<%= link_to 'Back', trackers_path %>