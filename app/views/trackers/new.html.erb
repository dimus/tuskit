<h1>New tracker</h1>

<%= error_messages_for :tracker %>

<% form_for(:tracker, :url => trackers_path) do |f| %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', trackers_path %>